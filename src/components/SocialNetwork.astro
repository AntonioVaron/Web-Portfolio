---
const { class: className, href, copy, id } = Astro.props;
---

<a
    class={`rounded-full border border-white/10 flex justify-center items-center gap-x-2 py-2 px-4 bg-white/5 hover:scale-110 hover:bg-white/10 transition text-xs lg:text-base ${className}`}
    href={href || "#"}
    {...(!copy ? { target: "_blank", rel: "noopener noreferrer" } : {onclick: "event.preventDefault(); copyToClipboard()", id: id})}
>
    <slot />
</a> 
